select * from brain_weights

select 
	gender,
	round(avg(size), 2)
from brain_weights
group by gender;

select 
	age,
	round(avg(size), 2)
from brain_weights
group by age;

select 
	gender,
	round(avg(weight), 2)
from brain_weights
group by gender;

select 
	age,
	round(avg(weight), 2)
from brain_weights
group by age;